{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run processor",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/processor/bin/processor",
            "args": [
                "--config",
                "${workspaceFolder}/processor/config-local.toml",
                "${workspaceFolder}/data/replays"
            ]
        }
    ]
}